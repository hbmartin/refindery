# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Cockpit admin UI bundle, injected into the wheel at release time from a
# pinned refindery-cockpit GitHub release (see COCKPIT_UI_VERSION). Never
# committed; a source checkout simply serves no /admin.
src/refindery/api/static/admin/

# Local runtime configuration and state
.env
.env.docker
data/
coverage.lcov
triage_decisions.db
reviews_triage/

# Zensical documentation build output and cache
site/
.zensical/
.coverage
